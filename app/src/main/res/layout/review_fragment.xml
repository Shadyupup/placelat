<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent" android:orientation="vertical">


    <Spinner
        android:id="@+id/googleSpinner"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:entries="@array/viewlist"
        android:prompt="@string/view1">
    </Spinner>
    <Spinner
        android:id="@+id/rate"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:entries="@array/ratelist"
        android:prompt="@string/rate1"
        android:layout_toRightOf="@id/googleSpinner">

    </Spinner>
    <TextView
        android:id="@+id/no"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="250dp"
        android:layout_marginLeft="150dp"
        android:text="@string/no"/>



    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerViewreviews"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        android:layout_below="@id/googleSpinner">
    </android.support.v7.widget.RecyclerView>

    <!--</RelativeLayout>-->


</RelativeLayout>
